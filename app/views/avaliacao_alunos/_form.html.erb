<%= form_with(model: avaliacao_aluno) do |form| %>
  <% if avaliacao_aluno.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(avaliacao_aluno.errors.count, "error") %> prohibited this avaliacao_aluno from being saved:</h2>

      <ul>
        <% avaliacao_aluno.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :pontos, style: "display: block" %>
    <%= form.text_field :pontos %>
  </div>

  <div>
    <%= form.label :observacoes, style: "display: block" %>
    <%= form.text_field :observacoes %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
